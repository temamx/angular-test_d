import { SortTitlePipe } from './sort-title.pipe';

describe('SortTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SortTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
